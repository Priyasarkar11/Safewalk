# Twilio or SMS provider credentials (do NOT commit real values)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM_PHONE=+1234567890

# Default recipient (or use a contacts store)
RECIPIENT_PHONE=+19876543210

# Server port
PORT=3000

# A simple shared token to protect the send-sms endpoint (replace with proper auth in prod)
API_SHARED_TOKEN=replace-me-with-secure-token